"""Agent-capability plugins: tools, providers, and MCP servers.

The UI is built in; what the *agent* can touch is pluggable (tools, model
providers, MCP servers). See loader.py for discovery and mcp.py for the
Model Context Protocol client.
"""
