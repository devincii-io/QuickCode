node_modules/
dist/
# Written by scripts/release.py alongside dist/; uploaded with the release,
# never committed — a checksum file in git is a second source of truth for
# bytes that live somewhere else.
SHA256SUMS.txt
*.log
.env
.quickcode/artifacts/
.quickcode/sessions/
.quickcode/tasks/
.quickcode/settings.local.json
__pycache__/
*.pyc
.venv/
*.egg-info/
.playwright-mcp/

# Local agent/session state — machine-specific, and a project's own
# .quickcode/settings.json can declare MCP servers, so it is never shared.
.claude/
.quickcode/
